<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Deja.Docs.Seo</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <!-- Reuses DocsNav/SeoMeta so the prerendered heads, sitemap and runtime tags share one source -->
  <ItemGroup>
    <ProjectReference Include="..\Deja.Docs\Deja.Docs.csproj" />
  </ItemGroup>

</Project>
